#pragma once
#include "bitxorcore/version/version.h"
#include <winresrc.h>

#if defined(BITXORCORE_VERSION_DESCRIPTION)
#define VER_FILE_DESCRIPTION_STR	BITXORCORE_VERSION_DESCRIPTION
#else
#define VER_FILE_DESCRIPTION_STR	""
#endif

#define VER_FILE_VERSION			BITXORCORE_VERSION_MAJOR, BITXORCORE_VERSION_MINOR, BITXORCORE_VERSION_REVISION, BITXORCORE_VERSION_BUILD
#define VER_FILE_VERSION_STR		BITXORCORE_BASE_VERSION

#define VER_PRODUCTNAME_STR			"bitxorcore"
#define VER_PRODUCT_VERSION			VER_FILE_VERSION
#define VER_PRODUCT_VERSION_STR		VER_FILE_VERSION_STR

#define VER_ORIGINAL_FILENAME_STR	STRINGIFY(WIN_TARGET_NAME)
#define VER_INTERNAL_NAME_STR		VER_ORIGINAL_FILENAME_STR

#define VER_COPYRIGHT_STR			BITXORCORE_COPYRIGHT

#ifdef _DEBUG
#define VER_FILEFLAGS				VS_FF_DEBUG
#elif !defined(BITXORCORE_BUILD_RELEASE)
#define VER_FILEFLAGS				VS_FF_PRIVATEBUILD
#else
#define VER_FILEFLAGS				0
#endif

#define VER_FILETYPE				WIN_FILETYPE
